# JOIN

# The join() method takes all items in an iterable and joins them into one string

l3 = ["NAFI", "NORI", "NSU", "UIU"]  # we have a list
joined = " and ".join(l3)  # each item of the list is joined together using " and " into one large string
print(joined)
